{
  "name": "beacon-connector",
  "packageManager": "pnpm@8.6.10",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "preview": "turbo run preview",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "tsconfig": "workspace:*",
    "turbo": "latest"
  }
}
